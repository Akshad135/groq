name: Workflow for PyPi

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.11.8

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python setup.py develop

      - name: Build package
        run: python setup.py sdist bdist_wheel

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          # password: ${{ secrets.PYPI_API_TOKEN }}
          password: pypi-AgEIcHlwaS5vcmcCJDQzYzRmZWQ1LTc3MTUtNDE2NS1iMDEyLTA4NzE0OWUzNTMxZgACKlszLCIzYjY5M2Y3ZC1lZjcxLTRiNzctYmY4Yy04MmNjNTBkZjZmODIiXQAABiBBfETi745dLSCOWCEMYKXY_6FL3Utgrcub7F_8EpXHpA
